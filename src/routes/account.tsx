import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/account")({ head: () => ({ meta: [{ title: "Account — DocuForge" }, { name: "description", content: "Manage account, security, and billing settings." }] }), component: Account });
function Account(){return <main className="min-h-screen bg-workspace px-4 py-12 text-foreground md:px-8"><div className="mx-auto max-w-4xl"><h1 className="text-5xl font-bold tracking-tight">Account</h1><div className="mt-8 grid gap-4"><section className="rounded-2xl border border-border bg-panel p-6 shadow-soft"><h2 className="text-xl font-semibold">Profile & security</h2><p className="mt-2 text-muted-foreground">Email login, Google login, password protection, reusable signatures, and language preferences.</p></section><section className="rounded-2xl border border-border bg-panel p-6 shadow-soft"><h2 className="text-xl font-semibold">Subscription</h2><p className="mt-2 text-muted-foreground">Plan limits, invoices, storage usage, and billing controls.</p></section></div></div></main>}
